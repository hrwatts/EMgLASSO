% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rmmnorm.R
\name{rmmnorm}
\alias{rmmnorm}
\title{Generate Samples from a Gaussian Mixture Model}
\usage{
rmmnorm(N, D, Tau, Mu, Sigma)
}
\arguments{
\item{N}{Integer. Number of observations to generate.}

\item{D}{Integer. Dimension of the distribution (number of features per observation).}

\item{Tau}{A numeric vector of length \eqn{k} specifying mixture proportions.
Must be non-negative and sum to 1. Will be normalized if not.}

\item{Mu}{A list of length \eqn{k}, each element a numeric vector of length \eqn{D}
specifying the mean of one mixture component.}

\item{Sigma}{A list of length \eqn{k}, each element a \eqn{D \times D}{D x D} covariance matrix
specifying the covariance of one mixture component.}
}
\value{
An \eqn{n \times D}{n x D} matrix of samples from the mixture model, where
\eqn{n \approx N} (exact value depends on rounding in allocation to components).
}
\description{
Generates random samples from a multivariate mixture of Gaussians (MMN) distribution
with specified mixture proportions, means, and covariance matrices.
}
\details{
Samples are generated by:
\enumerate{
\item Draw component allocations from a multinomial distribution with probabilities \eqn{\tau_j}{tau_j}.
\item Generate samples from each component using multivariate normal distribution.
\item Combine samples into a single matrix.
}
}
\examples{
\dontrun{
# Generate data from a mixture of 2 bivariate Gaussians
Tau <- c(0.3, 0.7)
Mu <- list(c(0, 0), c(5, 5))
Sigma <- list(diag(2), diag(2))
X <- rmmnorm(N = 100, D = 2, Tau = Tau, Mu = Mu, Sigma = Sigma)
}

}
