% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estep.R
\name{estep}
\alias{estep}
\title{E-Step of the EM Algorithm}
\usage{
estep(x, Theta, W)
}
\arguments{
\item{x}{An \eqn{n \times d}{n x d} matrix of observations.}

\item{Theta}{A list containing current parameter estimates with elements \code{Tau}
(mixture proportions), \code{Mu} (component mean vectors), and \code{Sigma}
(component covariance matrices).}

\item{W}{A list of covariance matrices to use for density evaluation (typically equals Theta$Sigma).
Used to allow flexible specification of the covariance structure.}
}
\value{
A list with \code{LogLike}, an \eqn{n \times k}{n x k} matrix of weighted densities,
and \code{Tnk}, an \eqn{n \times k}{n x k} matrix of posterior responsibilities.
}
\description{
Computes posterior responsibilities (cluster membership probabilities) for each observation
given the current parameter estimates. This is the expectation step of the EM algorithm.
}
\details{
The posterior responsibility is computed via Bayes' rule:
\deqn{T_{i,j} = \frac{\tau_j f(x_i; \mu_j, \Sigma_j)}{\sum_{\ell=1}^{k} \tau_\ell f(x_i; \mu_\ell, \Sigma_\ell)}}
}
\keyword{internal}
