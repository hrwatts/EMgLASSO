% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lstep.R
\name{lstep}
\alias{lstep}
\title{L-Step: Sparse Covariance Estimation via Graphical LASSO}
\usage{
lstep(x, Theta, lTol = 0.01, lMax = 3)
}
\arguments{
\item{x}{An \eqn{n \times d}{n x d} matrix of observations.}

\item{Theta}{A list of current parameter estimates with elements \code{Tau}
(mixture proportions), \code{Mu} (component mean vectors), and \code{Sigma}
(component covariance matrices).}

\item{lTol}{Numeric. Step size for the penalty grid. Default: \code{0.01}.}

\item{lMax}{Numeric. Maximum penalty value. Default: \code{3}.}
}
\value{
A list with \code{W}, the estimated sparse covariance matrices for each component,
and \code{BIC}, a \eqn{k \times r}{k x r} matrix of BIC values over the penalty grid.
}
\description{
Applies graphical LASSO regularization to estimate sparse precision (inverse covariance)
matrices. The regularization parameter is selected separately for each component using BIC.
}
\details{
For each component and penalty value \eqn{\rho}, the Bayesian Information Criterion is:
\deqn{BIC_\rho = -2 \log L(\hat{W}_\rho) + d_\rho \log(n)}
where \eqn{d_\rho} is the number of non-zero off-diagonal elements in the estimated
precision matrix and \eqn{n} is the sample size.

The penalty that minimizes BIC is selected for each component independently.
}
\references{
Friedman, J., Hastie, T., and Tibshirani, R. (2008). Sparse inverse covariance estimation
with the graphical lasso. \emph{Biostatistics}, 9(3), 432-441.
}
\keyword{internal}
